import styled from 'styled-components';

export const Wrapper = styled.div`
  height: 48px;
  .logo {
    flex-grow: 1;
    svg {
      width: 40px;
      height: 40px;
      transform: translateY(3px);
    }
  }
`;
