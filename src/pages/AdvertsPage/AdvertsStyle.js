import styled from "styled-components";

export const Filter = styled.div`
  input {
    height: 30px;
    border: none;
    padding: 0 10px;
    width: calc(100% - 80px);
    :focus {
      outline: none;
    }
  }
`;
